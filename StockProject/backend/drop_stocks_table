# drop_stocks_table.py
from database import SessionLocal, Stock, Base, engine

Base.metadata.drop_all(bind=engine, tables=[Stock.__table__])
print("✅ stocks 테이블 삭제 완료")

Base.metadata.create_all(bind=engine)
print("✅ 새 테이블 생성 완료")